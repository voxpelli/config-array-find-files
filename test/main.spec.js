import chai from 'chai';

// TODO: Replace with proper setup
import { main } from '../index.js';

chai.should();

describe('something', () => {
  it('should work', async () => {
    await main();
  });
});
